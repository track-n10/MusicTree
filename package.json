{
  "name": "music-link-finder",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.4.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run db:generate && npm run build -w @music-link-finder/db && npm run build -w @music-link-finder/core && npm run build -w @music-link-finder/adapters && concurrently \"npm run dev -w @music-link-finder/api\" \"npm run dev -w @music-link-finder/web\"",
    "build": "npm run build -w @music-link-finder/db && npm run build -w @music-link-finder/core && npm run build -w @music-link-finder/adapters && npm run build -w @music-link-finder/api && npm run build -w @music-link-finder/web",
    "lint": "npm run lint -w @music-link-finder/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "db:generate": "npm run db:generate -w @music-link-finder/db",
    "db:migrate": "npm run db:migrate -w @music-link-finder/db"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "dotenv-cli": "^11.0.0",
    "typescript": "^5.8.3"
  }
}
